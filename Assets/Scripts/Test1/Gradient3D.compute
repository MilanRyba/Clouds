#pragma kernel CSMain

RWTexture3D<float4> _Result;

int _Resolution;

[numthreads(8,8,8)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    float value = float(id.z) / _Resolution;
    _Result[id] = value;
}
